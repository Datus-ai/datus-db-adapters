[targets.postgresql]
package = "datus-postgresql"
kind = "compose"

[targets.mysql]
package = "datus-mysql"
kind = "compose"

[targets.clickhouse]
package = "datus-clickhouse"
kind = "compose"

[targets.starrocks]
package = "datus-starrocks"
kind = "compose"

[targets.doris]
package = "datus-doris"
kind = "compose"

[targets.tidb]
package = "datus-tidb"
kind = "compose"

[targets.trino]
package = "datus-trino"
kind = "compose"

[targets.greenplum]
package = "datus-greenplum"
kind = "compose"

[targets.hive]
package = "datus-hive"
kind = "compose"

[targets.spark]
package = "datus-spark"
kind = "compose"

[targets.oracle]
package = "datus-oracle"
kind = "compose"

[targets.gaussdb]
package = "datus-gaussdb"
kind = "compose"

[targets.hologres]
package = "datus-hologres"
kind = "cloud"

[targets.maxcompute]
package = "datus-maxcompute"
kind = "cloud"
# No repository credentials or live CI instance are currently available.
enabled = false

[targets.bigquery]
package = "datus-bigquery"
kind = "cloud"
# Enable after BIGQUERY_PROJECT, BIGQUERY_DATASET, and
# BIGQUERY_CREDENTIALS_INFO repository secrets have been configured.
enabled = false

[selection]
packages_without_live_targets = [
    "datus-clickzetta",
    "datus-db-core",
    "datus-redshift",
    "datus-snowflake",
    "datus-sqlalchemy",
]
